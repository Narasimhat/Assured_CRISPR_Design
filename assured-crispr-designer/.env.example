# No API keys required - all design runs in the browser.
#
# BUILD_SHA is optional: Vercel and GitHub Actions supply the commit automatically, and a
# local build reads it from git. Set it only when building outside a checkout.
# BUILD_SHA=
